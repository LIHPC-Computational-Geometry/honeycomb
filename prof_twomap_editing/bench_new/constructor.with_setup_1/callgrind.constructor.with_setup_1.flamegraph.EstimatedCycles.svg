<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="706" onload="init(evt)" viewBox="0 0 1200 706" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = true;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="706" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">prof_twomap_editing::bench_new::constructor with_setup_1:compute_dims(32)</text><text id="subtitle" x="50.0000%" y="48.00">callgrind.constructor.with_setup_1.flamegraph.EstimatedCycles.svg</text><text id="details" fill="rgb(0,0,0)" x="10" y="64.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="695.00"> </text><svg id="frames" x="10" width="1180" total_samples="265321"><g><title>all (265,321 EstimatedCycles, 100%)</title><rect x="0.0000%" y="76" width="100.0000%" height="15" fill="rgb(235,93,37)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="86.50"></text></g><g><title>(below main) [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="92" width="100.0000%" height="15" fill="rgb(213,116,39)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="102.50">(below main) [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>./csu/../csu/libc-start.c:__libc_start_main@@GLIBC_2.34 [/usr/lib/x86_64-linux-gnu/libc.so.6] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="108" width="100.0000%" height="15" fill="rgb(222,207,29)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="118.50">./csu/../csu/libc-start.c:__libc_start_main@@GLIBC_2.34 [/usr/lib/x86_64-linux-gnu/libc.so.6]</text></g><g><title>./csu/../sysdeps/nptl/libc_start_call_main.h:(below main) [/usr/lib/x86_64-linux-gnu/libc.so.6] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="124" width="100.0000%" height="15" fill="rgb(206,96,30)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="134.50">./csu/../sysdeps/nptl/libc_start_call_main.h:(below main) [/usr/lib/x86_64-linux-gnu/libc.so.6]</text></g><g><title>/home/runner/.cargo/registry/src/index.crates.io-6f17d22bba15001f/iai-callgrind-0.10.2/src/macros.rs:prof_twomap_editing::bench_new::run [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="140" width="100.0000%" height="15" fill="rgb(218,138,4)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="150.50">/home/runner/.cargo/registry/src/index.crates.io-6f17d22bba15001f/iai-callgrind-0.10.2/src/macros.rs:prof_twomap_editing::bench_new::run [target/release/deps/prof_twom..</text></g><g><title>/home/runner/.cargo/registry/src/index.crates.io-6f17d22bba15001f/iai-callgrind-0.10.2/src/macros.rs:prof_twomap_editing::main [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="156" width="100.0000%" height="15" fill="rgb(250,191,14)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="166.50">/home/runner/.cargo/registry/src/index.crates.io-6f17d22bba15001f/iai-callgrind-0.10.2/src/macros.rs:prof_twomap_editing::main [target/release/deps/prof_twomap_editing..</text></g><g><title>/rustc/07dca489/library/core/src/ops/function.rs:_ZN3std2rt10lang_start28_$u7b$$u7b$closure$u7d$$u7d$17h0533bf03d7c52522E.llvm.12836776482103543101 [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="172" width="100.0000%" height="15" fill="rgb(239,60,40)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="182.50">/rustc/07dca489/library/core/src/ops/function.rs:_ZN3std2rt10lang_start28_$u7b$$u7b$closure$u7d$$u7d$17h0533bf03d7c52522E.llvm.12836776482103543101 [target/release/dep..</text></g><g><title>/rustc/07dca489/library/core/src/ops/function.rs:std::rt::lang_start_internal [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="188" width="100.0000%" height="15" fill="rgb(206,27,48)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="198.50">/rustc/07dca489/library/core/src/ops/function.rs:std::rt::lang_start_internal [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>/rustc/07dca489/library/core/src/ops/function.rs:std::sys_common::backtrace::__rust_begin_short_backtrace [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="204" width="100.0000%" height="15" fill="rgb(225,35,8)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="214.50">/rustc/07dca489/library/core/src/ops/function.rs:std::sys_common::backtrace::__rust_begin_short_backtrace [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>/rustc/07dca489/library/std/src/rt.rs:_ZN3std2rt10lang_start28_$u7b$$u7b$closure$u7d$$u7d$17h0533bf03d7c52522E.llvm.12836776482103543101 [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="220" width="100.0000%" height="15" fill="rgb(250,213,24)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="230.50">/rustc/07dca489/library/std/src/rt.rs:_ZN3std2rt10lang_start28_$u7b$$u7b$closure$u7d$$u7d$17h0533bf03d7c52522E.llvm.12836776482103543101 [target/release/deps/prof_twom..</text></g><g><title>/rustc/07dca489/library/std/src/rt.rs:std::rt::lang_start_internal [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="236" width="100.0000%" height="15" fill="rgb(247,123,22)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="246.50">/rustc/07dca489/library/std/src/rt.rs:std::rt::lang_start_internal [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>/rustc/07dca489/library/std/src/sys_common/backtrace.rs:std::sys_common::backtrace::__rust_begin_short_backtrace [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="252" width="100.0000%" height="15" fill="rgb(231,138,38)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="262.50">/rustc/07dca489/library/std/src/sys_common/backtrace.rs:std::sys_common::backtrace::__rust_begin_short_backtrace [target/release/deps/prof_twomap_editing-244c6d5a75635..</text></g><g><title>0x0000000000020290 [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="268" width="100.0000%" height="15" fill="rgb(231,145,46)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="278.50">0x0000000000020290 [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]</text></g><g><title>honeycomb-utils/benches/core/twomap/editing.rs:iai_callgrind::bench::constructor [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="284" width="100.0000%" height="15" fill="rgb(251,118,11)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="294.50">honeycomb-utils/benches/core/twomap/editing.rs:iai_callgrind::bench::constructor [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>honeycomb-utils/benches/core/twomap/editing.rs:prof_twomap_editing::constructor::with_setup_1 [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="300" width="100.0000%" height="15" fill="rgb(217,147,25)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="310.50">honeycomb-utils/benches/core/twomap/editing.rs:prof_twomap_editing::constructor::with_setup_1 [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>main [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,321 EstimatedCycles, 100.00%)</title><rect x="0.0000%" y="316" width="100.0000%" height="15" fill="rgb(247,81,37)" fg:x="0" fg:w="265321"/><text x="0.2500%" y="326.50">main [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>honeycomb-core/src/twomap.rs:honeycomb_core::twomap::TwoMap&lt;_,T&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (265,157 EstimatedCycles, 99.94%)</title><rect x="0.0618%" y="332" width="99.9382%" height="15" fill="rgb(209,12,38)" fg:x="164" fg:w="265157"/><text x="0.3118%" y="342.50">honeycomb-core/src/twomap.rs:honeycomb_core::twomap::TwoMap&lt;_,T&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6] (261,866 EstimatedCycles, 98.70%)</title><rect x="1.3022%" y="348" width="98.6978%" height="15" fill="rgb(227,1,9)" fg:x="3455" fg:w="261866"/><text x="1.5522%" y="358.50">./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6]</text></g><g><title>/rustc/07dca489/library/alloc/src/alloc.rs:honeycomb_core::twomap::TwoMap&lt;_,T&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (125,649 EstimatedCycles, 47.36%)</title><rect x="52.6426%" y="364" width="47.3574%" height="15" fill="rgb(248,47,43)" fg:x="139672" fg:w="125649"/><text x="52.8926%" y="374.50">/rustc/07dca489/library/alloc/src/alloc.rs:honeycomb_core::twomap::TwoMap&lt;_,T&gt;..</text></g><g><title>__rust_alloc_zeroed [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (124,944 EstimatedCycles, 47.09%)</title><rect x="52.9084%" y="380" width="47.0916%" height="15" fill="rgb(221,10,30)" fg:x="140377" fg:w="124944"/><text x="53.1584%" y="390.50">__rust_alloc_zeroed [target/release/deps/prof_twomap_editing-244c6d5a756356ca]</text></g><g><title>/rustc/07dca489/library/std/src/alloc.rs:__rdl_alloc_zeroed [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (124,909 EstimatedCycles, 47.08%)</title><rect x="52.9216%" y="396" width="47.0784%" height="15" fill="rgb(210,229,1)" fg:x="140412" fg:w="124909"/><text x="53.1716%" y="406.50">/rustc/07dca489/library/std/src/alloc.rs:__rdl_alloc_zeroed [target/release/d..</text></g><g><title>/rustc/07dca489/library/std/src/sys/unix/alloc.rs:__rdl_alloc_zeroed [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (124,868 EstimatedCycles, 47.06%)</title><rect x="52.9370%" y="412" width="47.0630%" height="15" fill="rgb(222,148,37)" fg:x="140453" fg:w="124868"/><text x="53.1870%" y="422.50">/rustc/07dca489/library/std/src/sys/unix/alloc.rs:__rdl_alloc_zeroed [target/..</text></g><g><title>./malloc/./malloc/malloc.c:calloc [/usr/lib/x86_64-linux-gnu/libc.so.6] (124,855 EstimatedCycles, 47.06%)</title><rect x="52.9419%" y="428" width="47.0581%" height="15" fill="rgb(234,67,33)" fg:x="140466" fg:w="124855"/><text x="53.1919%" y="438.50">./malloc/./malloc/malloc.c:calloc [/usr/lib/x86_64-linux-gnu/libc.so.6]</text></g><g><title>0x000000000489b670 (124,494 EstimatedCycles, 46.92%)</title><rect x="53.0780%" y="444" width="46.9220%" height="15" fill="rgb(247,98,35)" fg:x="140827" fg:w="124494"/><text x="53.3280%" y="454.50">0x000000000489b670</text></g><g><title>honeycomb-core/src/dart.rs:honeycomb_core::dart::DartData&lt;_&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (95,048 EstimatedCycles, 35.82%)</title><rect x="64.1762%" y="460" width="35.8238%" height="15" fill="rgb(247,138,52)" fg:x="170273" fg:w="95048"/><text x="64.4262%" y="470.50">honeycomb-core/src/dart.rs:honeycomb_core::dart::DartData&lt;..</text></g><g><title>/rustc/07dca489/library/core/src/ptr/mod.rs:honeycomb_core::dart::DartData&lt;_&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (82,958 EstimatedCycles, 31.27%)</title><rect x="68.7330%" y="476" width="31.2670%" height="15" fill="rgb(213,79,30)" fg:x="182363" fg:w="82958"/><text x="68.9830%" y="486.50">/rustc/07dca489/library/core/src/ptr/mod.rs:honeyc..</text></g><g><title>/rustc/07dca489/library/core/src/ptr/mod.rs:honeycomb_core::twomap::TwoMap&lt;_,T&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (44,101 EstimatedCycles, 16.62%)</title><rect x="83.3782%" y="492" width="16.6218%" height="15" fill="rgb(246,177,23)" fg:x="221220" fg:w="44101"/><text x="83.6282%" y="502.50">/rustc/07dca489/library/co..</text></g><g><title>/rustc/07dca489/library/alloc/src/vec/mod.rs:honeycomb_core::dart::DartData&lt;_&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (11,116 EstimatedCycles, 4.19%)</title><rect x="95.8104%" y="508" width="4.1896%" height="15" fill="rgb(230,62,27)" fg:x="254205" fg:w="11116"/><text x="96.0604%" y="518.50">/rust..</text></g><g><title>/rustc/07dca489/library/alloc/src/vec/spec_from_iter.rs:&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (11,074 EstimatedCycles, 4.17%)</title><rect x="95.8262%" y="524" width="4.1738%" height="15" fill="rgb(216,154,8)" fg:x="254247" fg:w="11074"/><text x="96.0762%" y="534.50">/rust..</text></g><g><title>/rustc/07dca489/library/core/src/ptr/mod.rs:&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (10,415 EstimatedCycles, 3.93%)</title><rect x="96.0746%" y="540" width="3.9254%" height="15" fill="rgb(244,35,45)" fg:x="254906" fg:w="10415"/><text x="96.3246%" y="550.50">/rus..</text></g><g><title>__rust_alloc [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (1,629 EstimatedCycles, 0.61%)</title><rect x="99.3860%" y="556" width="0.6140%" height="15" fill="rgb(251,115,12)" fg:x="263692" fg:w="1629"/><text x="99.6360%" y="566.50"></text></g><g><title>/rustc/07dca489/library/std/src/alloc.rs:__rdl_alloc [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (1,624 EstimatedCycles, 0.61%)</title><rect x="99.3879%" y="572" width="0.6121%" height="15" fill="rgb(240,54,50)" fg:x="263697" fg:w="1624"/><text x="99.6379%" y="582.50"></text></g><g><title>/rustc/07dca489/library/std/src/sys/unix/alloc.rs:__rdl_alloc [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (1,619 EstimatedCycles, 0.61%)</title><rect x="99.3898%" y="588" width="0.6102%" height="15" fill="rgb(233,84,52)" fg:x="263702" fg:w="1619"/><text x="99.6398%" y="598.50"></text></g><g><title>./malloc/./malloc/malloc.c:malloc [/usr/lib/x86_64-linux-gnu/libc.so.6] (1,580 EstimatedCycles, 0.60%)</title><rect x="99.4045%" y="604" width="0.5955%" height="15" fill="rgb(207,117,47)" fg:x="263741" fg:w="1580"/><text x="99.6545%" y="614.50"></text></g><g><title>./malloc/./malloc/malloc.c:_int_malloc [/usr/lib/x86_64-linux-gnu/libc.so.6] (1,522 EstimatedCycles, 0.57%)</title><rect x="99.4264%" y="620" width="0.5736%" height="15" fill="rgb(249,43,39)" fg:x="263799" fg:w="1522"/><text x="99.6764%" y="630.50"></text></g><g><title>/rustc/07dca489/library/alloc/src/alloc.rs:honeycomb_core::dart::DartData&lt;_&gt;::new [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (811 EstimatedCycles, 0.31%)</title><rect x="99.6943%" y="636" width="0.3057%" height="15" fill="rgb(209,38,44)" fg:x="264510" fg:w="811"/><text x="99.9443%" y="646.50"></text></g><g><title>/rustc/07dca489/library/alloc/src/alloc.rs:&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter [target/release/deps/prof_twomap_editing-244c6d5a756356ca] (401 EstimatedCycles, 0.15%)</title><rect x="99.8489%" y="652" width="0.1511%" height="15" fill="rgb(236,212,23)" fg:x="264920" fg:w="401"/><text x="100.0989%" y="662.50"></text></g><g><title>./malloc/./malloc/malloc.c:sysmalloc [/usr/lib/x86_64-linux-gnu/libc.so.6] (329 EstimatedCycles, 0.12%)</title><rect x="99.8760%" y="668" width="0.1240%" height="15" fill="rgb(242,79,21)" fg:x="264992" fg:w="329"/><text x="100.1260%" y="678.50"></text></g></svg></svg>